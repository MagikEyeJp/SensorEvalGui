# Core scientific stack
numpy>=1.25,<2.0
matplotlib>=3.8,<4.0
tifffile>=2023.7
imagecodecs>=2023.7
roifile>=2023.9          # ← ROI ZIP / ImageJ .roi reader

# GUI
PySide6>=6.6,<6.7        # or swap for PyQt6 if preferred

# YAML handling
PyYAML>=6.0

# Development / tests
pytest>=8.0              # optional, unit tests
